setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE_URL
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - /scripts/init.sh
# no release config
# run is taken from Dockerfile
